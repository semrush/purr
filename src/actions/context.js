/**
 * Extension to add custom actions property.
 * @typedef ActionContext
 * @property {import('puppeteer').Browser} browser
 * @property {import('../browser/page').PageExtended} page
 */

module.exports = exports;
